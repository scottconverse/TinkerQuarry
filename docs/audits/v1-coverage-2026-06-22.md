# TinkerQuarry v1 coverage map - 2026-06-22 (verified, with corrections)

An exhaustive PRD-vs-code coverage map (6-agent workflow), then INDEPENDENTLY VERIFIED by the lead
before relaying. The CORE findings hold and match the external auditor: the engine is real and tested;
the front-end is manual-checked-only (no App.test.tsx, no browser flow); the signature Visual Correction
Loop is 0 lines; send-to-printer + post-print outcome have no front-end. **Three of the map's detailed
claims were WRONG on verification and are corrected here (the map can hallucinate evidence -- it also
self-flagged a phantom SendPanel.tsx):**

1. **Code drawer / "show me the code" (sec 6.5 / 7.7) is NOT missing.** The map claimed the generated
   SCAD is "never loaded into Monaco / unreachable." False: engineDocument.ts (describeIntoStudio:106,
   reopenIntoStudio:139) calls engine.source, and setEngineDocument (line 111 -> 121 updateFileContent)
   loads it into the active document, which Monaco renders -- verified LIVE (a 20mm cube showed
   `width=20.0; difference(){...}` in the Editor tab). The agent grepped only direct App.tsx callers and
   missed the indirection. The code drawer works.
2. **First-run tool-health citation (sec 7.1) is phantom.** The map cited FirstRunWizard.tsx -- that file
   does not exist. The underlying point (no first-run tool-health gate) is UNVERIFIED; there is managed
   model-download onboarding, but a tool-health gate was not confirmed. Treat as unverified, not proven.
3. **Version history "persists one snapshot, overwritten on save" (sec 6.12) is misleading.**
   design_store.save writes per design-id, so DISTINCT saved designs persist as separate gallery entries
   (verified: two coexisting). Only RE-saving the SAME design overwrites. The real gap is per-design
   *iteration* history (every refine), which is in-session (undo) only -- not persisted.

Everything else below stands. The blunt verdict in sec 4 is accurate.

---

Inline correction applied 2026-06-22: the generated SCAD source method is loaded through
`engineDocument.ts`, not directly by `App.tsx`. The earlier sentence claiming generated code is
unreachable was wrong. Treat the code drawer as working for generated source, with the remaining gap
being edit/rerun through the engine gate and stronger automated coverage.

---

# TinkerQuarry v1 Coverage Assessment — Un-spun (verified 2026-06-22)

## 1. Bottom line

Of the **86 mapped v1-blocking requirements**, roughly **40% are genuinely built + automated-tested** (almost entirely the *engine* and the *Studio-inherited viewer/editor*), **~12% are built but manual-only** ("verified LIVE" = one screenshot, no automated front-end test), **~30% are partial** (engine capability exists, front-end is absent, stubbed, or non-functional), and **~18% are missing outright**. The single most important honest sentence: **the product's signature differentiator — the §6.3.1 Visual Correction Loop — does not exist as code (`packages/engine/src/kimcad/pipeline.py:944` returns at the geometric gate; no view-capture, no vision critique, no rounds, no convergence, no logging), its PRD acceptance test cannot even be run, and the entire send-to-printer + post-print-outcome surface that closes the "make it real" promise has zero front-end implementation, so v1 is not close on the dimensions that make TinkerQuarry *TinkerQuarry*, even though the manufacturing engine underneath it is real and well-tested.**

A correctness note the auditor should know: several mapped rows cite `apps/ui/src/components/SendPanel.tsx` (the §6.10 "maturity state" rows). **That file does not exist** — `find apps/ui/src -iname "*send*"` returns nothing and `grep` finds zero callers of `engine.send`/`engine.outcome`. Those line-numbered citations are phantom; the same JSON correctly marks the actual "Send a sliced job" and Screen-12 rows as `missing`. Treat every "SendPanel.tsx:NNN" maturity-state citation as describing engine/`engineClient.ts` capability with **no shipping UI**.

---

## 2. The complete remaining-work list for v1

Everything below is v1_blocking and NOT built-automated-tested. Three rows the JSON marks `v1_blocking:false` are deliberately excluded (2D SVG measure, orientation-chip dynamic text, typed `stale` slice reason, printer capabilities display, software-verified maturity label, done-state celebration UX, branching history, iteration-log §6.12 advanced, theme picker, advanced/dev toggles, full-shell a11y, printer CRUD). What remains is the honest "what's left."

### A. MISSING (unbuilt — zero or dead code)

| ref | capability | one-line evidence |
|---|---|---|
| §6.3 tool-using agent | "watch it work" narrated agent (drawing/looking/fixing) | No tool loop in engine; fixed labels only `pipeline.py:71-72`, `designPhase.ts:6-15`; grep for narrative phrases = 0 |
| §6.3.1 when-it-runs | VCL on codegen path | Loop absent; `_run_llm_backend` ends at gate `pipeline.py:944` |
| §6.3.1 inputs | multi-view render capture for critique | No render-to-views step; grep front/back/top/iso in pipeline = 0 |
| §6.3.1 budget | separate visual-round budget (default 3) | Only `max_render_retries=2` exists `pipeline.py:386` (render-error, not visual) |
| §6.3.1 best-candidate | keep best render across rounds | `pipeline.py:944` returns last; no scoring/retention |
| §6.3.1 convergence | approved / best-after-max / fatal exit states | No such states; gate-complete or render_failed only |
| §6.3.1 modes | full-visual / text-only / off labeled in UI | No VCL-mode UI anywhere |
| §6.3.1 logging | per-round iteration log | No iteration log; `PrintRecord` is coarse only |
| §6.3.1 user control | accept / force-round / stop + dev inspector | Design-level cancel only `App.tsx:422-424` |
| §6.3.1 acceptance | wrong-face hole MUST be flagged | **CANNOT BE MET** — gate is pure math `pipeline.py:933,988`; local vision 0/3 (`vision-spike.md`); cloud path unbuilt |
| §7.5 Visual Correction view | the whole screen | grep VisualCorrection = 0; signature feature, zero code |
| §13-B / §13-C VCL | loop as first-class v1 feature + round states | `honesty-audit-2026-06-22.md:31` "Zero code" |
| §6.3.1 when-it-skips | template parts skip the loop | No loop to skip |
| §6.8 / §7.10 orient override | user-rotatable pose | grep orient.override = 0; no `/api/orient`; engine cap unused by FE |
| §6.10 / §7.12 send | send sliced job to printer w/ confirm | `engine.send` defined `engineClient.ts:159`, **0 callers**; "Make it real" only downloads .gcode.3mf |
| §6.10 / §7.13 outcome | "How did it print?" prompt | `engine.outcome` defined, **0 callers**; endpoint `webapp.py:1994` never hit from FE |
| §6.11 / §13-A bundled libraries | BOSL2, Round-Anything, YAPP_Box, Catch'n'Hole, gridfinity-rebuilt, MCAD bundled; thread support replaced cleanly | **Updated after Codex takeover:** seven libraries are now vendored under `packages/engine/library/vendor/` with pinned attribution and smoke-render proof, including clean-room MIT `tq-threads` pinned to commit `c6e54ed60cacb7ecb58cc6e0a09c252434306c31` (post-`v0.3.0` docs/provenance follow-up) as the Dan/rcolyer replacement. Dan Kirshner `threads.scad` remains excluded because the available source is GPL-3.0-or-later. |
| §13-C external-lib admit | consent→sandbox→sanitize→include-path | Dead registry; `connector.py:62-66` says admission is "next slice"; renderer hard-codes `_APPROVED_PREFIX="library/"` |
| §6.12 / §7.16 iteration log | "what was tried" log w/ gate verdicts per round | No IterationLog component; `history.py` stores print outcomes only |
| §6.12 version history (persist) | reopen restores prior versions | Saved designs persist and reopen, but per-design iteration history is not persisted. Re-saving the same design overwrites that saved design; distinct saved designs coexist. |

### B. PARTIAL (capability real, but front-end absent / stubbed / non-functional)

| ref | capability | one-line evidence |
|---|---|---|
| §6.3 model picker | obvious non-jargony local-model selector | Only a cloud/local *toggle* + OpenRouter key; no in-flow chooser; local model id fixed server-side |
| §6.3 explain mode | user-invoked "explain this design" | Only an auto-emitted post-generate dimensions toast; grep user-facing "explain" = 0 |
| §6.3 diff edits | partial edits w/ per-change rollback | Whole-design undo only `App.tsx:864-876` (session-only); compare card is manual viz, no structural diff |
| §6.3.1 open-spike | local-vs-cloud vision quality decision shipped | Spike done, decision made; **no code for either path** |
| §6.5 edit + re-run | edited SCAD re-enters gate without AI | `manualRender` wired `App.tsx:386,2444,2741` for params; free-form source→re-gate path not confirmed wired |
| §6.6 clamped values | engine adjusted-params surfaced to user | FE clamp is client-side input only `ParameterControl.tsx:390`; engine `adjusted_params` not rendered |
| §6.9 slice profiles | plain-language profile line pre-slice (layer height) | **Updated after Codex takeover:** printer/material picker already existed; `/api/options` now carries `layer_height_mm` for profile display before slicing; slice responses add structured `estimate_detail.layer_height_mm`; and the Ready-to-print line includes layer height after a successful slice. |
| §6.10 printer mgmt | multi-protocol connection UI in send flow | 6 connectors + `ConnectionsCard` exist, **not wired into any Send flow** |
| §6.10 printer status | ready/busy/offline/needs_setup live display | Enum + labels exist; no real-time display (no Send UI) |
| §6.10 maturity: connected | live status during send | `connectorStatus.ts:41` label exists; SendPanel referenced in JSON does not exist |
| §6.10 maturity: first-real-send | escalated send caution | `FirstRealPrintDialog` covers the *slice* moment, not physical send |
| §6.10 maturity: printing-progress | live job % | Engine computes 0-1; FE has no progress bar (no Send UI) |
| §6.11 auto-wire includes | auto `use<lib>` injection | Works for KimCad's first-party modules and nested vendored library paths; bundled vendor entries are advertised in the manifest and sandbox-approved. |
| §6.11 / §6.14 / §7.14 external-lib chooser | Settings manage external roots | `LibrariesSettings.tsx` UI exists but backend never admits roots to renderer |
| §6.13 / §6 export formats | .scad/.stl/.3mf/.png/.step/.svg/.dxf | PNG **not selectable** (confirmed: 0 png in `ExportDialog.tsx`); .scad only via File▸Save; .step conditional; export mock-tested only |
| §6.14 per-task model | stronger model for visual loop | One cloud slug + one local model; no per-task picker |
| §6.14 privacy panel | telemetry-off + disclosure | Present and correct (telemetry genuinely absent) — but tagged v1_blocking partial; effectively done, low risk |
| §6.1 / §7.1 first-run tool health | health check + "check again" during setup | No current first-run wizard exists to host this; health checks live in Settings only. The requirement remains unmet, but prior citations to `FirstRunWizard.tsx` are invalid because that file is absent. |
| §7 Screen 1 first-run | tool-health within setup | Same gap as above |
| §7 Screen 3 workspace | continuous manufacturing rail | Rail split across Inspector tabs; orient-override absent; code drawer absent |
| §7.14 settings libs/advanced | external-lib admission | UI present, admission logic missing |
| §9 vision-unavailable label | "text-only" labeling | Moot — loop doesn't exist |
| §9 override framing | "you're overriding a safety check" | Soft "proceed with caution" only; no hard-fail override; no orient override |
| §13-A manufacturing rail | readiness/orient/slice/send/printer end-to-end | Readiness ✓, Slice ✓(download), Orient ✗, Send ✗, Printer ✗ |
| §13-B slice+send+outcome | the make-it-real triad | Slice ✓; Send ✗; Outcome ✗ |

### C. BUILT-BUT-UNVERIFIED (real, manual-only — no automated FE test)

| ref | capability | one-line evidence |
|---|---|---|
| §6.3 multi-turn refine | plain-English refine ("make it taller") | Wired `App.tsx:674-730,881-886`, threaded `pipeline.py:445`; verified live once; **no FE automation** asserting returned geometry |
| §6.4 offscreen multi-view | N-angle capture | `offscreenRenderer.ts:54-200` real, used by ShareDialog — but **orphaned**; the consuming VCL doesn't exist |
| §6.7 gate-fail reasons | plain-English why + one-step fix | `ReadinessPanel.tsx` renders it; manual-only |
| §6.7 "Ready" gating | shown only on pass/override | `App.tsx:706-717`; verified live once |
| §6.6/§6.12 customizer live re-render | debounced re-render + re-gate | Engine determinism tested; FE re-gate effect `App.tsx:916` untested (mock returns `{ok:true}`) |
| §6.4 viewer full workflow | build-plate/presets/ortho/wireframe/section/measure on a real design | Studio features tested in isolation; **no test POSTs `/api/design`→renders in viewer** |
| §7.18 offline banner | network-loss banner | `EngineStatusBanner` flags engine-down, not `navigator.onLine`; manual stop/restart only |
| §12 reliability | FE orchestration degrades gracefully | Engine safety solid; **no `App.test.tsx`** — undo/make-it-real/re-gate React glue untested |
| §13 FE test coverage | browser-level user-flow test | `engineLive.integration.test.ts` is API-only (its own comment); all FE tests stub fetch |

---

## 3. Net-new findings (not in prior docs/audits or the owner's auditor's list)

1. **Phantom citations in the requirements map itself.** The §6.10 maturity-state rows cite `apps/ui/src/components/SendPanel.tsx:182/194/255/258` etc. as evidence of "built-automated-tested" states. **That file does not exist** (`find` returns nothing; `engine.send`/`engine.outcome` have 0 callers). Any reader trusting those rows would over-credit the send surface. The honest reading collapses all six "maturity state" rows into the single fact: engine + `engineClient.ts` methods exist, **no send UI ships**. The prior honesty-audit said the loop is missing but did **not** flag that the map's own evidence lines point at a non-existent file.

2. **Generated design code is reachable; the earlier wiring finding was wrong.** `engine.source()` is
used through `engineDocument.ts` to populate the Studio editor. The remaining risk is not absence of a
code drawer; it is edit/rerun through the engine gate and browser-level coverage of that path.

3. **The offscreen multi-view renderer is built and working but orphaned.** `offscreenRenderer.ts` (7 preset views) is real and shipping via ShareDialog — yet it is the *exact* input the (missing) Visual Correction Loop would consume. The owner is paying the maintenance cost of half the loop's plumbing while getting none of the loop's value. No prior doc framed it as "the capture half exists, the brain half is zero."

4. **First-run tool-health remains unmet, but the prior file citation was invalid.** There is no
`FirstRunWizard.tsx` in the current app. Health detection exists in Settings; there is no setup-time
tool-health surface with "check again."

5. **Version history is weaker than the PRD, but not as broken as first stated.** Distinct saved designs
persist and reopen. The real gap is per-design iteration history: meaningful refine/tune attempts are
not persisted as a durable timeline, and re-saving the same design overwrites that design's saved state.

---

## 4. Blunt verdict

**The product is not close to v1, and the signature value is absent.** What is real is the *factory floor*: the engine slices real motion-bearing G-code, the gate refuses bad parts server-side fail-closed, save/reopen/source round-trips under real HTTP tests, security (per-boot token, SCAD sandbox, loopback-only) is genuinely tested, and the Studio-inherited 3D viewer is solid. That is a credible, trustworthy CAD-to-G-code engine. But TinkerQuarry's *reason to exist* — an AI that **looks at the render and catches spatial errors math can't** — is zero code, its acceptance test (wrong-face hole) **cannot be run**, the local vision model already failed it 0/3, and the punted "cloud-optional" path was never written. Below that, the "make it real" arc dead-ends at a file download: there is **no send-to-printer UI and no post-print-outcome prompt** despite tested engine endpoints sitting unused. And the entire front-end is structurally unverified — **no `App.test.tsx`, no test that POSTs a real design and renders it** — which is the precise gap that let the §6.12 reopen bug ship before. Call it honestly: a strong v1 *engine* wearing a v0.6 *application*, missing its one differentiating feature.

Key files: `C:\Users\Scott\Desktop\CODE\tinkerquarry\packages\engine\src\kimcad\pipeline.py:944` (gate-end, no loop); `C:\Users\Scott\Desktop\CODE\tinkerquarry\apps\ui\src\services\engineClient.ts:159` and `:168` (send/outcome/source client methods); `C:\Users\Scott\Desktop\CODE\tinkerquarry\apps\ui\src\services\engineDocument.ts` (generated source wiring); `C:\Users\Scott\Desktop\CODE\tinkerquarry\packages\engine\library\` (first-party modules only; bundled third-party libraries absent); `C:\Users\Scott\Desktop\CODE\tinkerquarry\docs\audits\vision-spike.md` (0/3 acceptance failure); `C:\Users\Scott\Desktop\CODE\tinkerquarry\docs\audits\honesty-audit-2026-06-22.md:23` (prior verdict).
