# Stage 5 — Next-Sprint Watchlist (forward-looking)

Structural / decision items that don't block the Stage 5 gate but should be tracked. (For this project the gate fixes everything in-sprint; these are the *durable* concerns the audit surfaced for later cycles.)

| Item | Origin | Why it's here | Trigger to act |
|---|---|---|---|
| **Per-id re-render lock** | ENG-503 | `render_lock` is process-global; invisible for the single-user local UI, but it serializes re-renders across *all* designs. The invariant only needs per-design-dir mutual exclusion. | Any networked / multi-client / shared-server mode. Key the lock by `rid` (mirrors the `slice_lock` refactor). |
| **`BBoxTerm` linear model vs non-linear module geometry** | ENG-501 | The one crack in the pure-data-families design is where a linear `BBoxTerm` sum can't express a module's non-linear behavior (`max(2, …)` in `hooks.scad`). Patched by raising the slider min; the *seam* remains as families grow. | Adding a family whose module bbox isn't a linear function of its params → add a small per-family analytic override hook. |
| **`<1 s` performance regression gating** | TEST-003 / DOC-004 | The interactive target is now asserted on the live gate (`all_meet_target`), but the hard CI ceiling stays 5 s to avoid flake. A slow box could keep PASS-ing the ceiling while a family creeps over 1 s on slower hardware. | Run the bench on Kim's target box; if margins tighten, add a per-target perf budget. |
| **Viewport / `KCViewport` test harness** | TEST-007 | The viewport-reload-on-`?v=` link rests on code inspection (three.js/WebGL isn't run in jsdom — a deliberate no-browser-E2E boundary). The api-layer `?v=` stripping *is* tested. | When a headless-WebGL or Playwright harness is stood up (likely around the Stage-10 export/print UI), add a `loadMesh`-called-on-version-change test. |
| **Plural/alias robustness as families grow** | ENG-504 | `_singular` strips a trailing `s`; correct for today's aliases, fragile for a future alias ending in `s` (`truss`→`trus`). A collision test is added now as insurance. | Adding any family alias ending in `s` → confirm the collision test still passes. |
| **Build-volume gate-fail reachability in demo/QA** | QA-001 | The `snap_box` clamp bounds (≤250 mm) sit under the smallest printer envelope (256³), so the build-volume gate-fail can't be tripped via the demo template. Safe by design; means a manual "drag past the plate" QA script won't fire it. | A future template family whose `max` can exceed a configured printer's smallest axis → ensure the live gate-fail path is QA-reachable. |
| **Per-axis slider↔viewport bridge (W/D/H chips)** | UX-004 | The prototype tied each dimensional slider to the viewport's W/D/H pills via a small axis chip; dropped in the shipped sliders. A scannability nicety, not a gap. | Restored this stage if cheap (expose `bbox_axis`); otherwise a polish item for the next UI pass. |
